apiVersion: apps/v1
kind: Deployment
metadata:
  name: astitva-eta-deploy
  labels:
    app: astitva-eta-deploy-lbl
spec:
  replicas: 3
  selector:
    matchLabels:
      app: astitva-eta-app
  template:
    metadata:
      labels:
        app: astitva-eta-app
    spec:
      containers:
      - name: astitva-eta-container
        image: astitva911/application-img1.0
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: astitva-eta-np-service
  labels:
    app: astitva-eta-np-app
spec:
  selector:
    app: astitva-eta-deploy-lbl

  type: NodePort
  ports:
  - nodePort: 31028
    port: 8080
    targetPort: 8080
